<%= form_for @user, url: wizard_path, method: :put do |f| %>
  <h2>Set Your Department</h2>

  <%= f.label :department, 'Select your department:' %>
  <%= f.collection_select :department_id, Department.all, :id, :name, prompt: 'Select department' %>

  <%= f.submit 'Next' %>
<% end %>