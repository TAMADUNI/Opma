<% provide(:title, "home") %>
<h1>Home#index</h1>

<%= link_to 'Home', root_path %>

<% if user_signed_in? %>
  start the timer
  <%= button_to 'Sign out', destroy_user_session_path, method: :delete %>
<% else %>
   <h3>Please login to start activities</h3>
<% end %>